<h1>Articles</h1>

<ul>
  <% @articles.each do |article| %>
    <li>
      <%= article.id %>
      <%= article.title %>
      <%= article.body %>
    </li>
  <% end %>
</ul>

<%= link_to "New article", new_article_path(@article) %>
<%= link_to "Home page", root_path() %>

